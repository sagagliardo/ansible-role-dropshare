- name: Install PIP
  script: "{{ pip_script_location }}"

- name: install testinfra
  pip:
    name: testinfra

- name: run tests
  script: "testinfra -v {{ item }}"
  with_items: "{{ tests_to_run }}"
